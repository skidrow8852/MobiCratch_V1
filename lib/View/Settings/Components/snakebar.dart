import 'package:flutter/material.dart';

class SnakeBarMsg extends StatelessWidget {
  const SnakeBarMsg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
